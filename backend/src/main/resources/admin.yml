management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      show-details: always
#logging:
#  pattern:
#    file: -%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID}){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx
#  file:
#    name: /Users/myungjun/Desktop/logs/logback/client.log
#    name: 본인 경로 적기